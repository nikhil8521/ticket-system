version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - JWT_SECRET=your_super_secret_key_change_this
      - DB_PATH=ticket.db
    volumes:
      - ./data:/app/database
    restart: always
